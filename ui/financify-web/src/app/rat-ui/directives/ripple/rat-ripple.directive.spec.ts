import { RatRippleDirective } from './rat-ripple.directive';

describe('RatRippleDirective', () => {
/*  it('should create an instance', () => {
    const directive = new RatRippleDirective();
    expect(directive).toBeTruthy();
  });*/
});
